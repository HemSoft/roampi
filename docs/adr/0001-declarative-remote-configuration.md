# Keep remote configuration declarative

RoamPi accepts versioned JSON that selects fixed native components and declares remote prompts or commands, but it never downloads executable UI code or stores approval in the remote file. This keeps App Store behavior, validation, and trust decisions inside the reviewed app binary while still allowing each machine and project to customize its interface.
